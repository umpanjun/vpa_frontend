# 🌐 API base URL
# ใช้ /api เพราะ nginx.conf proxy /api → backend:3000 ให้แล้ว
VITE_API_BASE=/api

# ✅ เปิดส่ง cookie/jwt ข้าม domain
VITE_WITH_CREDENTIALS=true

# 🌍 Base URL ของระบบ (ถ้า deploy จริงเปลี่ยนเป็น domain/subdomain)
VITE_API_BASE=http://147.50.253.67
